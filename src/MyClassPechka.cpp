/*
#include <MyClassPechka.h>

class Pechka;

double Pechka::getTemp(String val){
    if(val == "term")
        return (tempterm.GetAverageTemp(analogRead(temp_sensor_pech))- delta_termistr);
    else if(val == "ds")
        return dataBuf["digit_temp"];
    else
        return -127;
}
*/
